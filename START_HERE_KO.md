# 안티그래비티에서 시작하기

이 ZIP은 모토웍스 AI 개발 소스 스냅샷입니다. 운영 DB, 사진 원본, API 키, node_modules, Git 이력은 포함하지 않습니다. 원본 커밋: 1eb8b3b.

## 여는 방법

1. Windows의 C:\Projects 등에 압축을 풉니다.
2. package.json이 있는 motoworks-ai 폴더를 안티그래비티에서 엽니다.
3. 아래 지시문을 에이전트 채팅창에 넣습니다.

## 첫 지시문

이 프로젝트는 기존 모토웍스 AI의 개발 소스다. 기존 운영본을 변경하지 말고 로컬 개발용 Git 저장소와 별도 작업 브랜치를 준비해 줘. 먼저 실제 코드와 실행 환경을 확인하고 변경 계획을 보여줘.

React/TypeScript/Vinext/Vite, Cloudflare Workers, D1(DB), R2(FILES) 기반 구조를 보존해 검토해 줘. .openai/hosting.json에는 기존 운영 프로젝트 식별자가 남아 있으므로 그 대상으로 배포하거나 운영 리소스를 연결하지 마. 별도 로컬 개발 환경에서만 진행해.

Gemini 3.8 Flash 판독 코드가 있지만 실제 API와 운영 환경 검증은 완료되지 않았다. 모델 지원 여부, 필요한 비밀 환경변수, 로컬 인증 및 DB 초기화 방법을 먼저 확인해. 플랫폼 전용 의존성 설치가 막히면 대체 설치를 임의 진행하지 말고 원인과 변경 영향을 알려줘. API 키는 채팅으로 요청하거나 로그에 출력하지 마.

최초 관리자 대상은 shortsbogo@gmail.com만 유지해. 담당자의 기능 권한·허용센터와 정비 건의 실제 작업센터를 분리해. 자양센터와 용전센터를 모두 처리할 수 있어야 해.

아래 남은 항목을 실제 코드 기준으로 우선 점검하고, 사진 업로드→AI 판독→검수→승인→새로고침 후 복원→엑셀까지 안전하게 연결해. 운영 배포와 데이터 이전은 별도 승인 전까지 금지해.

## 현재 소스에서 특히 점검할 항목

- 사용자 역할별 기능 권한 분리와 센터별 목록 조회 제한: 현재 단순 역할 존재 확인만으로는 충분하지 않음.
- 관리자 직접 권한 그룹 생성·할당 화면과 서버 로직: 확정 요구사항이나 완성됐다고 간주하면 안 됨.
- 승인 처리: review_tasks 업데이트와 정비·작업·결제·매출 반영을 원자적으로 연결하고 중복 승인을 막아야 함.
- 목록 복원: 현재 GET /api/extractions는 pending만 조회하므로 승인내역과 수정값 복원 보강 필요.
- 일부 대시보드·고객·정산·권한 화면은 예시 데이터가 남아 있음. 실제 데이터로 오인되지 않도록 분리 필요.
- 수정 필드의 ID·키를 서버 저장값과 대조하고, 개인정보 암호화 누락·평문 fallback을 방지해야 함.
- 문서 사진 좌표 표시, 필수값·금액 검증, API 제한·실패 재시도·중복 처리 보강 필요.
- 기존 5개 시트 엑셀 원본이 없어 정확한 호환성은 미검증.
- README 및 PRD의 완료 표현보다 실제 구현·테스트를 우선할 것.

## 환경 참고

package.json의 Node 요구 버전은 >=22.13.0입니다. .env.example은 빈 키를 가진 설정 예시이며 실제 비밀값이 아닙니다. 환경변수 전달 방식은 Cloudflare 로컬 실행 환경에 맞게 확인하세요.

기존 자동 검증은 12개 테스트와 타입 검사·빌드 통과 수준입니다. 실제 Gemini 응답 정확도, 운영 DB 연동, 브라우저 전체 업무 흐름 검증 완료를 뜻하지 않습니다.
