# [모토웍스 AI] 현장 수기 영수증 자동화와 보안 실증: AI 기반 이륜차 정비 ERP 구축기

---

## 1. 프로젝트 한 줄 브리핑 및 배경
**"현장 정비명세서와 영수증 사진을 AI로 판독하고 검수하여, 승인된 데이터만 정비 주문과 매출로 연결합니다."**

모토웍스 AI는 대전 용전센터, 서울 자양센터(2개 센터) 등 현장에서 발생하는 수기 정비명세서 및 영수증을 Google Gemini AI로 판독하고, 이를 정산·고객 관리·엑셀 ERP 시스템과 연동하는 정비 솔루션입니다.

개인정보(PII)의 AES-GCM 암호화 저장, HMAC-SHA256 세션 쿠키 기반의 인증, 다중 역할 권한 분리(RBAC)를 적용하여 현재 상용 도메인(`https://motoworks.corepartners.kr`)에 배포되어 있으며, 12주 현장 파일럿 검증을 목표로 고도화 중입니다.

---

## 2. 해결하고자 하는 현장의 핵심 과제 (Pain Points)

1. **수기 정비명세서 입력 부담**:
   * 현장 정비 후 손글씨로 작성된 부품명, 공임비 등을 본사 관리자가 시스템에 일일이 재입력하는 시간과 오탈자 발생.
2. **이륜차 번호판 식별 및 차량 매칭**:
   * 이륜차 번호판의 다양한 표기(지역명, 공백 등) 속에서 정확한 4자리 일련번호를 추출하고 기존 등록 차량과 안전하게 매칭해야 함. (※ 현재 실제 번호판 사진 대상 정량적 판독률 측정 진행 중)
3. **개인정보 유출 방지 및 보안**:
   * 고객 연락처, 차량번호 등이 평문으로 저장되거나 감사 로그에 노출되지 않도록 암호화 및 마스킹 처리 필수.
4. **역할별 권한 통제**:
   * 본사 관리자, 지점 매니저, 정비사 간의 권한을 명확히 분리하여 승인되지 않은 데이터가 매출에 임의 반영되지 않도록 통제.

---

## 3. 모토웍스 AI의 핵심 기술 아키텍처

### ① Gemini AI 기반 정비명세서 판독 및 인간 검수 (Human-in-the-loop)
* **AI 판독 모델**: Google Gemini 모델을 활용하여 정비일, 고객명, 연락처, 차종, 번호판, 작업 항목, 금액 추출.
* **사람 검수 원칙**:
  * AI가 판독한 결과는 즉시 확정되지 않고 '검수 대기함(Review Tasks)'에 등록됨.
  * 담당자가 검수 화면에서 원본 사진과 판독값을 대조하여 수정 및 최종 승인.
  * **승인 전 데이터는 절대 매출이나 통계에 반영되지 않는** 원칙 준수.

### ② 차량 번호판 정규화 및 단방향 해시 색인
* 번호판 텍스트에서 숫자 일련번호를 추출(`extractPlateDigits`)하고, HMAC-SHA256(`hashPlateDigits`)을 이용해 `plate_digits_hash` 색인을 생성.
* 개인정보 보호를 위해 번호판 및 고객 연락처는 AES-GCM-256(`v1:iv:ciphertext`)으로 암호화하여 DB에 저장.

### ③ 인증 계층 및 RBAC 권한 분리
* **세션 쿠키 인증**:
  * Web Crypto HMAC-SHA256 서명 기반 `motoworks_session` 쿠키 발급.
  * 로그인 실패 5회 시 15분 차단(Rate Limit) 및 타이밍 공격 방지 바이트 비교(`timingSafeEqual`) 적용.
* **비밀번호 단방향 해싱**:
  * 표준 `PBKDF2-HMAC-SHA256` (100,000회 반복, 16바이트 솔트) 해싱 적용.
* **다중 역할(RBAC) 분리**:
  * `admin`: 전체 관리 및 사용자 생성/삭제 (`manage_users`)
  * `shop_manager`: 지점 관리, 정비 승인(`review_decide`), 엑셀 내보내기, 사용자 목록 조회(`view`)
  * `staff`: 사진 업로드(`upload`), 판독값 수정(`edit_extraction`), 목록 조회(`view`)
  * `viewer`: 단순 열람

### ④ 클라우드 인프라 구성
* **Cloudflare Workers**: 서버리스 분산 엣지 환경에서 구동.
* **Cloudflare D1**: 관계형 SQLite 데이터베이스로 주문, 정비항목, 권한, 감사 로그 관리.
* **Cloudflare R2**: 업로드된 정비명세서 원본 이미지 파일 비공개 보관.
* **클라이언트 이미지 최적화**: 모바일 현장에서 대용량 사진 업로드 시 Canvas 기반 사전 압축(`optimizeReceiptImage`) 적용.

---

## 4. 실전 검증 결과

* **자동화 테스트 검증**:
  * Vitest 기반 **7개 테스트 스위트, 59개 단위/통합 테스트 전원 통과**.
  * 로컬 Miniflare D1 환경에서 권한 제어, 지점 데이터 격리, 승인 후 데이터 복호화, 신규 차량 등록 후 `lookup-plate` 단일 매칭 왕복 검증 통과.
* **이륜차 번호판 AI OCR 판독률**:
  * **실제 번호판 사진 기반 판독률 측정 예정 (현재 미측정)**.
  * 현장 실차 번호판 표본(반사광, 오염, 각도 등)을 확보하여 실측 데이터가 나오는 대로 수치를 반영할 예정.

---

## 5. 관리자 및 사용자 계정 관리

* **웹 관리 화면**: `[+ 사용자 추가]` 모달을 통해 관리자가 안전하게 신규 직원을 등록.
* **100% 대화형 안전 터미널 CLI (`scripts/create-user.mjs`)**:
  * 셸 히스토리에 비밀번호가 남지 않도록 `stdin` 프롬프트로만 입력받아 초기 관리자 및 관계자 계정 생성 지원.

---

## 6. 향후 계획 및 합의된 로드맵

현재 번호판 조회, 명세서 AI 판독, R2 원본 보관, 검수·승인 기반 정비·결제 데이터 생성 파이프라인이 구축되어 있습니다.

* **P0 보완 과제 (진행 중)**:
  - 번호판 촬영 결과와 명세서 차량번호의 서버 측 불일치 차단.
  - 현장 즉석 수기 접수 API (`POST /api/orders`) 구현 (정비사 등록 시 검수 대기 진입 원칙 준수).
* **Phase 2 합의 로드맵**:
  - 고객 카카오 알림톡 기반 소모품 교체 주기 알림 발송 연동.
  - 2개 센터 중심의 12주 현장 파일럿 실증 및 상용성 검증.
